{
    "scripts": {
      "heroku-prebuild": "cd frontend && npm install && cd ../backend && npm install-client && npm install",
      "heroku-postbuild": "cd frontend && npm run build && mv build ../backend",
      "start": "cd backend && node app.js"
    },
    "engines": {
        "node": "v14.13.1"
    }
}