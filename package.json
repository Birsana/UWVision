{
    "scripts": {
      "heroku-prebuild": "cd frontend && npm install && npm run build && mv build ../",
      "heroku-postbuild": "cd backend && npm run install-client && npm install",
      "start": "cd backend && node app.js"
    },
    "engines": {
        "node": "v14.13.1"
    }
}